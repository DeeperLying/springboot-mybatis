server:
    port: 8081
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://127.0.0.1:3306/lee?useUnicode=true&characterEncoding=UTF-8&useSSL=false
        username: root
        password: Localhost
    data.redis:
        host: localhost
        port: 6379
        password:
        database: 0
mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.example.demospringboot.entity
    configuration:
        map-underscore-to-camel-case: true
